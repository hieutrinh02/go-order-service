apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: order-service

resources:
  - namespace.yaml
  - configmap.yaml
  - postgres
  - redis
  - kafka
  - migrate
  - api
  - publisher
  - consumer
  - prometheus
  - grafana
